# Upstash Redis – REST URL + Token from console.upstash.com (Connect → REST tab).
# Use the MAIN token (not Read-Only). Uncheck "Read-Only Token" when copying.
KV_REST_API_URL=
KV_REST_API_TOKEN=
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=

# Base URL for paste links. Vercel par VERCEL_URL auto. Local: APP_URL (quotes nahi).
APP_URL=
